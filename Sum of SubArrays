class Solution {
    public int subarraySum(int[] arr) {
       
        int total = 0;
        for (int i = 0; i < arr.length; i++) {
            total += arr[i] * (i + 1) * (arr.length - i);
            
        } 
        return total;
        
    }
}
 
